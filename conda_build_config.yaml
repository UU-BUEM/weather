# Conda-build configuration for reproducible local builds.
# Ensures `conda build .` can resolve weather runtime deps from conda-forge.
channel_sources:
  - conda-forge,defaults

channel_targets:
  - conda-forge main

# Explicit numpy variant removes fallback warning while keeping current default.
numpy:
  - 1.26
